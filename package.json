{
  "name": "azury",
  "scripts": {
    "pre:dev": "concurrently \"npm run compile\" \"npm run serve\"",
    "dev": "tauri dev",
    "serve": "live-server dist --port=5555 --no-browser",
    "compile": "node scripts/build.mjs",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.0-rc.5",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0-rc.11",
    "@types/react": "^18.0.9",
    "@types/react-dom": "^18.0.5",
    "concurrently": "^7.2.1",
    "esbuild": "^0.14.41",
    "esbuild-css-modules-plugin": "^2.2.16",
    "live-server": "^1.2.2",
    "typescript": "^4.7.2"
  }
}
